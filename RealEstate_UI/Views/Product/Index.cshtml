@model List<ResultProductDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
}
<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">İlan Listesi</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Başlık</th>
                                <th scope="col">Şehir</th>
                                <th scope="col">İlçe</th>
                                <th scope="col">Kategori</th>
                                <th scope="col">Fiyat</th>
                                <th scope="col">Tür</th>
                                <th scope="col">Güncelle</th>
                                <th scope="col">Sil</th>
                                <th scope="col">Detaylar</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var x in Model)
                            {
                                <tr>
                                    <th scope="row">@x.productID</th>
                                    <td>@x.title</td>
                                    <td>@x.city</td>
                                    <td>@x.district</td>
                                    <td>@x.categoryName</td>
                                    <td>@x.price</td>
                                    <td>@x.type</td>
                                    <td><a href="/Product/UpdateProduct/@x.productID" class="btn btn-outline-success">GÜNCELLE</a></td>
                                    <td><a href="/Product/DeleteProduct/@x.productID" class="btn btn-outline-danger">SİL</a></td>
                                    <td><a href="#" class="btn btn-outline-info">DETAYLAR</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Product/CreateProduct/" class="btn btn-outline-secondary">Yeni İlan Girişi</a>
                </div>
            </div>
        </div>
    </div>
</div>


